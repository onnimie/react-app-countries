const SearchOutput = (props) => {
    return (
        <ul>
            {props.countryNames.map(name => 
                <li>
                    name
                </li>
            )}
        </ul>
    )
}


export default SearchOutput